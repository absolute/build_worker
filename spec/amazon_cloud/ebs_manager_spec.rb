require "spec_helper"

describe "EBSManager" do
  context "with a new project" do
    it "should create a new volume"
    it "should return volume details"
  end
  context "with a volume that has no active project associated with it" do
    it "should delete that volume"
  end
end